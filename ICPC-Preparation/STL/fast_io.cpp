// Fast IO helpers
#include <bits/stdc++.h>
using namespace std;

static inline void fast_io() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
}

// Example: read int quickly
static inline int read_int() {
    int x;
    cin >> x;
    return x;
}
